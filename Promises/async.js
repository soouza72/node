// PROMISES -> SIMPLES.JS

const { reject } = require("async");
const { resolve } = require("path");

/* Vamos criar um exemplo que simula buscar dados de um usuario em um banco de dados, a busca vai levar 2 segs */
function buscarUsuario(id) {
    return new Promise()
}